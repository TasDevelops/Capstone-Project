# Capstone-Project
I created a lambda function for each task as well as api gateways. All works ok however if an external tries to make the html link work, 
they may encounter an access denied error, even though I made it all public on CORS.
